language: node_js
node_js:
  - '0.12'
  - '0.10'

before_install:
  - sudo add-apt-repository -y ppa:ubuntu-wine/ppa
  - sudo apt-get update
  - sudo apt-get -y install wine1.7
  - npm install -g grunt-cli
